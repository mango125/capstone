@{
    ViewData["Title"] = "컴퓨터 쇼핑몰";
}
@model List<Member>


@section Scripts {
    <script>
        $(document).ready(function () {
            $("#executeQueryButton").click(function () {
                // 쿼리 실행 코드를 여기에 추가
                $.get("/ControllerName/ActionName", function (data) {
                    // 결과를 처리하는 코드
                });
            });
        });
    </script>
}

<div class="container-fluid">
    <div class="bg d-flex align-items-center justify-content-center">
        <span class="text-white display-1 d-flex align-items-center justify-content-center">메인페이지 </span>

        @*테스트용 버튼 생성*@
       @* @foreach (var member in Model)
        {
            <tr>
                <td>@member.id</td>
                <td>@member.pw</td>
                <td>@member.name</td>
            </tr>
        }*@
        <div>
        </div>
    </div>
</div>

@*<nav>
    <div class="container-fluid">
        <div class="row">
            <!-- siderbar-->
            <div class="col-lg-3 sidebar fixed-top">
                <a href="#" class="navbar-brand text-white text-center d-block mx-auto py-3 mb-4
                bottom-border">Com Shop</a>
            </div>
            <!-- end of sidebar-->
            <!-- top navbar-->
            <div class="col-lg-9">
            </div>
            <!-- end of top navbar -->

        </div>
    </div>
</nav>*@